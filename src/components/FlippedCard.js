import React from "react";

const FlippedCard = ({ robots }) => {
  <HeroCardWrapper>
    <img src={images} alt="heroes" />
    <div className="info">
      <h2>{name}</h2>
    </div>
  </HeroCardWrapper>;
};
